import { StyleSheet, Text, View } from "react-native";
import React from "react";

const InputDecorator = () => {
  return (
    <View>
      <Text style={{ fontSize: 50, color: "red" }}>InputDecorator</Text>
    </View>
  );
};

export default InputDecorator;

const styles = StyleSheet.create({});
