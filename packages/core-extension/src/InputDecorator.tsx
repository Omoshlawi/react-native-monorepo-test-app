import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ExtensionsApp = () => {
  return (
    <View>
      <Text style={{ fontSize: 50, color: "green" }}>ExtensionsApp</Text>
    </View>
  );
};

export default ExtensionsApp;

const styles = StyleSheet.create({});
